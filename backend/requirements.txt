# Web framework
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Claude Agent SDK (wraps the Claude Code engine; needs Node + @anthropic-ai/claude-code CLI)
claude-agent-sdk>=0.1.0

# Data / ORM
SQLAlchemy>=2.0
pydantic[email]>=2.7
pydantic-settings>=2.3

# Auth
python-jose[cryptography]>=3.3
argon2-cffi>=23.1
cryptography>=42.0  # Fernet encryption for stored secrets (Anthropic key, SMTP password)

# Validation / templating
jsonschema>=4.22
Jinja2>=3.1

# Document ingestion
pdfplumber>=0.11
pypdf>=4.2
python-docx>=1.1
openpyxl>=3.1

# SRS rendering: DOCX via python-docx (above); PDF via reportlab (pure-Python, always installs).
reportlab>=4.2
markdown>=3.6
# Optional, nicer PDF styling. Needs native GTK/Pango libs and often fails to install on
# Windows, so it is NOT installed by default — srs_output.py automatically uses reportlab when
# WeasyPrint is absent. To opt in: `pip install weasyprint`.
# weasyprint>=62.0
